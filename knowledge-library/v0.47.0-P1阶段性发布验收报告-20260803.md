# v0.47.0 P1 阶段性发布验收报告（S9–S13 单 Agent 接线）

日期: 2026-08-03
版本: v0.47.0-dev（基线 v0.46.1）
范围: v0.47 P1 单 Agent 治理接线闭环（S9–S13）发布前验收
前置: P0（S1–S8）已验收并提交（10 commits）

## 结论

**P1 单 Agent 接线（S9–S13）验收通过**，可进入 P1 联邦治理（F1–F4）。本阶段不包含
F1–F4 与 R1（属 P1/P2 后续），版本 bump 与 CHANGELOG 按规划在 M4 发布阶段执行。

## 验收矩阵（对照规划 §4）

| 验收维度 | 状态 | 依据 |
|---------|------|------|
| 联邦传输认证 | ✅ | `test_federation_auth_ssrf`：无签名/错签名→401 + `auth_failed` 审计；有效签名→200；in-process 兼容 |
| SSRF | ✅ | `peer_url` 回环/链路本地/file:// → 400；白名单放行 |
| 策略 fail-closed | ✅ | `test_policy_fail_closed`：无规则→DENY + `no_rule_match`；4 冲突策略保持绿 |
| 信任报告 | ✅ | `test_trust_report_signing`：伪造 source_server 签名→丢弃 + `rejected_reports` 审计 |
| sidecar 认证 | ✅ | `test_api_auth_fail_closed`：无 key→拒绝；`allow_unauthenticated` 开发旗标；`/_debug/` 不可绕过 |
| /api/mcp 治理 | ✅ | `test_mcp_entrypoint_governed`：tools/call 走三层治理；真实 SecurityGate 拦截 `maref_exec` |
| 单 Agent 接线（S9） | ✅ | `test_pipeline_trust_boundary`：越界动作经 pipeline→DENY + `matched_rule=trust_boundary` + 审计回调 |
| 行为闭环（S10） | ✅ | `test_behavior_probe_assembly`：sidecar 装配探针 start→采集→检测→扣信任→熔断链路可观测 |
| 预检闸门（S11） | ✅ | `test_harness_preflight_wiring`：preflight 走 TaskPreflight 6 项；风险授权硬门禁（FAIL→abort） |
| scope 防伪（S12） | ✅ | `test_scope_subject_binding`：伪造签发者→拒绝；subject_did≠agent_id→拒绝 |
| Judge 接线（S13） | ✅ | `test_judge_wiring`：Trace 走真实仲裁；`record_call` 回写 accuracy |
| 联邦闭环 / 监管执行 | ⬜ **未纳入本阶段** | F2 共识成员认证 / R1 监管 ENFORCE 属后续 P1/P2 |
| 回归 | ✅ | 全量专项 95 passed；综合回归 2167 passed（governance+loop+execution+agent+sidecar+联邦+安全） |

## 专项验收（§4.1）

| 交付物 | 状态 | 证据 |
|--------|------|------|
| S1 | ✅ | auth_failed 审计存在（federation_http.py:263），客户端自动签名测试 |
| S2 | ✅ | 三类恶意 URL 拒绝 |
| S3 | ✅ | no_rule_match 审计 + 4 策略保持绿 |
| S6 | ✅ | 请求级拒绝（规划"或"方案）+ 开发旗标 |
| S7 | ✅ | 两入口统一治理 + 真实拦截 |
| S9 | ✅ | E1006 语义→DENY + 审计回调 |
| S12 | ✅ | 伪造签发者/主体不匹配均拒绝 |

## 风险缓解核对（§5.1）

| 风险 | 状态 |
|------|------|
| R1 联邦认证回归 | ✅ S1 两步走（开关→fail-closed），e2e 全量回归通过 |
| R2 TrustBoundary 误伤 | ⚠️ 保持默认严格（cross_org 拒绝，已有测试锁定）；`allowed_domains` 参数支持生产显式放宽——比规划"默认加入 global/cross_org"更保守，安全优先 |
| R3 伪签名协议兼容 | ⚠️ 未保留 HMAC 兼容层（无生产 A2A 对端依赖旧 HMAC，实测零调用方受影响）；若未来外部对端出现需补弃用兼容层 |
| R4 Judge 误报 | ✅ RuleJudge 仅 BLOCK 明确模式硬裁决；FLAG 转人工复核（HITL），不自动否决 |
| R5 人日超期 | ✅ P0 与 P1 流水线推进，每项独立 commit |
| R6 覆盖率 | ⚠️ 全仓覆盖率未达 60%（规划 M4 处理，不阻塞本阶段） |

## 质量门禁

- 专项验收测试 95 passed；综合回归 2167 passed（剩 10 项为基线已确认失败，与本次无关）
- ruff strict **0 errors**（全改动域）；mypy strict **Success**（60 文件）
- 工作区干净；提交全部使用 `feat:`/`security:` 前缀（版本纪律合规）

## 里程碑状态

- v0.47.0-M1（P0 S1–S8）✅ 已完成
- v0.47.0-M2（S9–S13 + F1–F3）🔄 S9–S13 完成，F1–F3 待实施
- v0.47.0-M3 / M4 ⬜ 待实施

## 下一步

- **F1 multi_agent 编排治理化**（2.0 人日）：`MultiAgentCoordinator` 接入联邦治理——
  派发前经 TrustBoundaryManager、任务带审计/计量、异常走级联断路器；补单元+集成测试（当前 0 测试）
- F2 共识成员认证（依赖 S1 基础设施）
- F3 联邦派发经 TrustBoundary
