# MAREF v0.12.0-rc: 深度自演进 10 轮实施方案

**计划日期**: 2026-05-08  
**起始版本**: v0.11.0-rc (R60)  
**目标版本**: v0.12.0-rc (R70)

---

## 一、v0.11.0-rc 经验总结

### 成果

| 维度 | R51 基线 | R60 终态 |
|------|----------|----------|
| 测试数 | 2341 | 2354 |
| 代码变更 | 0 | 17 files, +731/-87 lines |
| Git 标签 | 0 | 10 (r51→rc) |
| SelfHealer 执行层 | 模拟 | 真实 subprocess |
| SelfOptimizer 执行层 | 硬编码假数据 | 真实 pytest+coverage |
| 治理参数 | 15 个保守默认 | 15 个激进调整后锁定 |
| 混沌测试 | 43 (chaos only) | 89 (chaos+stress+security) |
| 代码演进 | 不可用 | 沙箱验证通过 |

### 有效模式

1. **可注入回调模式**: `strategy_executor`、`benchmark_fn`、`re_diagnose` 等回调参数在保持生产代码真实的同时让测试保持快速。这是本次执行中最成功的架构决策。

2. **参数演进顺序**: 先治理层再学习层的两波策略避免了耦合——治理参数影响全局行为，学习参数影响自适应速度。如果同时修改二者，因果链不可追踪。

3. **逐轮回归验证**: 每轮结束后运行相关测试套件，R55 做全量回归。R12 test_r12_audit.py 的 HealAction exit_code 断言失败在 R55 就被捕获，而非等到 R60。

### 反模式与局限

4. **测试反向调用真实测试的问题**: `_run_real_benchmark()` 从 pytest 内部调用子进程 pytest 导致超时。需要测试专用作用域参数。当前被 skip。

5. **SelfArchitect 仍然薄弱**: 依赖分析是真实的（AST），但 `propose_redesign()` 产生的依然是描述性字符串，无法被 SelfExecutor 的 CodeGenerator 转化为可部署代码。这是从 R58"受限代码演进"变为"验证而非生成"的根本原因。

6. **6 个模块的执行层仍是模拟的**: ContinuousOptimizer.sandbox_test 仍返回 `simulated_gain`，EvolutionDSL.simulate 默认返回硬编码指标，SelfOptimizer.run_experiment 的 apply_fn 未被真实调用。

### 关键教训

> **安全网过度建设，执行层建设不足**——v0.11.0-rc 证明了五层防御体系足够坚固，但自改进模块产生有意义变更的能力仍是瓶颈。下一阶段的核心任务是：缩短"诊断→提案→生成→部署→验证"闭环中"提案→生成"段的距离。

---

## 二、剩余差距

v0.11.0-rc 审计中识别出 6 个仍处于模拟/半模拟状态的模块：

| 模块 | 当前状态 | 阻塞 v0.12.0 什么 |
|------|----------|-------------------|
| ContinuousOptimizer | `sandbox_test()` 假增益 | 无法真实测量优化效果 |
| EvolutionDSL | `simulate()` 默认假指标 | 无法预测参数变更后果 |
| SelfArchitect | `propose_redesign()` 字符串占位 | 无法生成 SelfExecutor 可用的代码提案 |
| SelfHealer.heal_cycle | `re_diagnose` 可选，默认不调用 | 无法自动验证治愈是否成功 |
| SelfOptimizer.run_experiment | `apply_fn` 可选，默认不调用 | 优化=测量，但不会真正应用变更 |
| ResilienceV2 | `execute_degradation_plan` 需要外部回调 | 降级计划不会自动触发 |

---

## 三、v0.12.0-rc: R61-R70 十轮演进

### 版本路线

```
v0.11.0-rc (R60) ──→ R61-R70 → v0.12.0-rc
```

### 阶段划分

| 阶段 | 轮次 | 主题 |
|------|------|------|
| Phase A: 闭环补全 | R61-R63 | 关闭提案→生成差距，真实化剩余模拟模块 |
| Phase B: 真实自演进 | R64-R66 | 首次真实代码生成+部署+验证闭环 |
| Phase C: 深度验证 | R67-R69 | 覆盖率追踪、跨模块混沌恢复、性能基线 |
| Phase D: 发布 | R70 | 全量验证 + v0.12.0-rc |

---

### R61 | SelfArchitect 重写：从字符串到结构化提案

**当前问题**: `propose_redesign()` 产生 `"refactor_SelfHealer"` 这类字符串。CodeGenerator 无法从字符串生成代码。

**变更**: 重新设计 `ArchitectureProposal` 使其包含 CodeGenerator 可消费的结构化字段。

```
ArchitectureProposal 新增字段:
  target_files: list[str]         # 具体文件路径
  change_type: enum               # ADD_TEST | EXTRACT_FUNCTION | REMOVE_UNUSED_IMPORT | SPLIT_MODULE
  affected_symbols: list[str]     # 类名/函数名
  estimated_new_lines: int
  preconditions: list[str]        # 执行前必须满足的条件
```

**SelfArchitect 新增方法**:

| 方法 | 作用 |
|------|------|
| `analyze_low_coverage()` | 用 coverage report 发现真实低覆盖率模块 |
| `detect_unused_imports()` | AST 扫描全项目，聚合 file→unused_imports 映射 |
| `propose_test_addition()` | 对低覆盖率模块生成 ADD_TEST 类型提案 |
| `propose_import_cleanup()` | 对未使用导入生成 REMOVE_UNUSED_IMPORT 类型提案 |

- [ ] ArchitectureProposal 新增 5 个结构化字段
- [ ] 3 个新增 propose 方法
- [ ] `propose_redesign()` 改为聚合上述方法
- [ ] 更新 test_r18_r20.py 测试
- [ ] Tag: v0.12.0-rc-r61

---

### R62 | ContinuousOptimizer + SelfOptimizer 链路真实化

**当前问题**: `sandbox_test()` → `measure()` → `adopt()` 链路中 sandbox_test 返回假增益。

**变更**: 将 ContinuousOptimizer 与 SelfOptimizer 的 `_run_real_benchmark()` 连接。

| 文件 | 变更 |
|------|------|
| `continuous_optimizer.py:sandbox_test()` | 接收可选的 `benchmark_fn` 参数，调用真实基准 |
| `continuous_optimizer.py:measure()` | 使用 sandbox_result 中的真实 before/after 指标 |
| `continuous_optimizer.py:run_cycle()` | 传递 benchmark_fn |
| `self_optimizer.py` | `_run_real_benchmark()` 增加 `--durations=0` 性能模式 |

**新增 ContinuousOptimizer.__init__ 参数**: `benchmark_fn: Callable | None = None`

当提供 benchmark_fn 时，sandbox_test 执行真实基准（作用域到特定文件）；否则使用模拟模式。

- [ ] sandbox_test 真实化（可选）
- [ ] measure 使用真实指标
- [ ] `_run_real_benchmark` 性能模式
- [ ] 更新 test_r34_continuous_optimizer.py
- [ ] Tag: v0.12.0-rc-r62

---

### R63 | EvolutionDSL + SelfHealer 闭环验证

**当前问题**: `simulate()` 默认假指标；`heal_cycle()` 不自动重新诊断。

**变更**:

| 文件 | 变更 |
|------|------|
| `evolution_dsl.py:simulate()` | 当提供 benchmark_fn 时填充真实 test_pass_rate/coverage/perf |
| `evolution_dsl.py:SafetyGate.evaluate()` | 新增 test_pass_rate ≥ 0.95、coverage_drop ≤ 2%、perf_regression ≤ 5% 检查 |
| `self_healer.py:heal_cycle()` | 新增 `auto_re_diagnose: bool = False` 参数。为 True 时自动构造 re_diagnose 回调 |

- [ ] simulate() 真实 benchmark 集成
- [ ] SafetyGate 扩展检查（3 项扩展）
- [ ] heal_cycle auto_re_diagnose 模式
- [ ] 更新相关测试
- [ ] Tag: v0.12.0-rc-r63

---

### R64 | CodeGenerator 升级：从 Stub 到真实代码

**当前问题**: `CodeGenerator._generate_refactor()` 只产生 `R31AutoGenerated` stub。无法生成可运行代码。

**变更**: CodeGenerator 根据 ArchitectureProposal 的 `change_type` 生成不同代码。

| change_type | 生成逻辑 |
|-------------|----------|
| ADD_TEST | 生成 pytest 测试函数骨架（含 fixture、有效断言） |
| EXTRACT_FUNCTION | 生成提取后的函数（保留签名、添加文档字符串） |
| REMOVE_UNUSED_IMPORT | 生成删除导入的新版本文件内容 |
| SPLIT_MODULE | 生成拆分为两个文件的方案 |

```
实现策略: 使用 ast.parse() 读取目标文件，基于 AST 生成修改，而非字符串替换。
这样保证语法正确性。
```

- [ ] CodeGenerator 4 种 change_type 的代码生成逻辑
- [ ] AST 操作而非字符串替换
- [ ] 生成的代码通过 ASTSandbox 验证
- [ ] 更新 test_r31_self_executor.py
- [ ] Tag: v0.12.0-rc-r64

---

### R65 | 首次真实代码部署（安全沙箱内）

**目标**: 利用 R61-R64 的能力，在 git 分支上执行一次完整闭环：
SelfArchitect 提案 → CodeGenerator 生成 → ASTSandbox 验证 → SafetyGate 审核 → AtomicDeployer 部署 → pytest 验证 → 通过保留/失败回滚

**前置条件**:
- [ ] 在专用分支 `feature/self-evolution-r65`（非 main）
- [ ] R61-R64 全量测试通过
- [ ] SelfExecutor.dry_run() 先验证

**部署内容**: 向覆盖率最低的模块添加一个真实可运行的测试文件。

**成功标准**:
- 新测试文件通过 `pytest --tb=short`
- 覆盖率上升 ≥1%
- 0 回归失败

- [ ] Git 分支创建
- [ ] 完整闭环执行
- [ ] 部署前后覆盖率对比
- [ ] Tag: v0.12.0-rc-r65

---

### R66 | ResilienceV2 自动化降级响应

**当前问题**: `execute_degradation_plan()` 需要外部传入回调，无法自动触发。

**变更**: ResilienceEvaluatorV2 获得自动连接能力。

| 新增 | 作用 |
|------|------|
| `ResilienceEvaluatorV2.attach_circuit_breaker(cb)` | 治理降级自动触发 CB.force_open() |
| `ResilienceEvaluatorV2.attach_collector(collector)` | 观测降级自动触发 collector.stop() |
| `ResilienceEvaluatorV2.evaluate_and_respond(factors)` | 评估→推荐→自动执行的一体化方法 |

- [ ] 3 个 attach 方法 + evaluate_and_respond
- [ ] 更新 test_r14_r17.py
- [ ] Tag: v0.12.0-rc-r66

---

### R67 | 覆盖率追踪系统

**目标**: 建立跨轮次的覆盖率趋势追踪，使 SelfOptimizer 和 ContinuousOptimizer 有真实数据可优化。

**新增模块**: `src/maref/recursive/coverage_tracker.py`

```
CoverageTracker:
  - record(snapshot_id, coverage_pct, per_module_coverage)
  - trend() → 斜率 + p-value
  - low_coverage_modules(threshold=80) → list[str]
  - compare(snapshot_a, snapshot_b) → 增量变化
```

**集成点**: SelfOptimizer._run_real_benchmark 调用后自动记录；ContinuousOptimizer 读取趋势判断饱和。

- [ ] CoverageTracker 模块
- [ ] SelfOptimizer 集成
- [ ] ContinuousOptimizer 集成
- [ ] 测试
- [ ] Tag: v0.12.0-rc-r67

---

### R68 | 跨模块混沌恢复实战

**目标**: 注入真实的跨模块故障（非模拟），验证 SelfHealer 的 6 个真实策略能否恢复。

**测试场景**:

| 场景 | 注入方式 | 期望恢复策略 |
|------|----------|-------------|
| 删除 __init__.py | `os.remove` | import_error → check_missing_dependency_install → 重建 |
| 引入语法错误 | 写入无效 .py | test_failure → rerun_tests_with_verbose → 识别+修复 |
| 破坏 pip 依赖 | `pip uninstall -y pydantic` | dependency_conflict → pip check → pip install |
| 删除 .coverage | `os.remove` | coverage_drop → coverage run |
| 快速参数震荡 | PolicySandbox 20次/60s | oscillation 检测 → PolicySandbox auto-revert |

**成功标准**: 每个场景 SelfHealer 识别正确策略 + 真实恢复 + SelfDiagnostician 确认 HEALTHY。

- [ ] 5 个真实故障场景的测试
- [ ] 恢复时间测量
- [ ] 更新 test_r3_healing.py 新测试类
- [ ] Tag: v0.12.0-rc-r68

---

### R69 | 性能基线与优化

**目标**: 建立性能基线，用 SelfOptimizer 的真实基准功能测量优化效果。

**基线测量**:
```
pytest tests/ --durations=20 → 记录最慢 20 个测试
覆盖率 → 记录 per-module 覆盖率
状态机延迟 → GovernanceStateMachine.transition() 1000 次均值
审计日志延迟 → AuditLogger.log() 1000 次均值
```

**优化方向**: ContinuousOptimizer 在真实 benchmark_fn 模式下运行 3 个周期，观察 gain。

- [ ] 性能基线采集
- [ ] ContinuousOptimizer 3 周期真实运行
- [ ] 基线与优化后对比报告
- [ ] Tag: v0.12.0-rc-r69

---

### R70 | 收敛验证 + v0.12.0-rc 发布

**操作清单**:
1. 全量 pytest 收集（预期 2400+）
2. coverage --fail-under=85（基线 80→85）
3. CoverageTracker 趋势报告
4. ResilienceEvaluatorV2 终态评分
5. SelfHealer 真实恢复成功率统计
6. 更新 pyproject.toml: version = "0.12.0-rc"
7. 补充 CHANGELOG.md: v0.11.0-rc → v0.12.0-rc
8. git tag: v0.12.0-rc
9. 合并 feature/self-evolution-r65 → main（如成功）

- [ ] 全量测试
- [ ] 覆盖率
- [ ] CHANGELOG
- [ ] pyproject.toml version
- [ ] Tag: v0.12.0-rc

---

## 四、十轮汇总

| 轮次 | 阶段 | 核心动作 | 解决哪个模拟 | 测试预期 |
|------|------|----------|-------------|----------|
| R61 | 闭环补全 | SelfArchitect 结构化提案 | propose_redesign 占位 → 结构化 | 2380 |
| R62 | 闭环补全 | CO+SO 链路真实化 | sandbox_test 假增益 → 真实 | 2385 |
| R63 | 闭环补全 | DSL simulate + Healer 闭环 | 假指标 + 不重诊断 | 2390 |
| R64 | 真实演进 | CodeGenerator 升级 stub→真实代码 | _generate_refactor stub | 2395 |
| R65 | 真实演进 | 首次真实代码部署闭环 | apply_fn 从未调用 | 2400 |
| R66 | 真实演进 | ResilienceV2 自动化降级 | 降级计划需手动回调 | 2405 |
| R67 | 深度验证 | CoverageTracker 新建 | 覆盖率无追踪 | 2415 |
| R68 | 深度验证 | 跨模块混沌恢复实战 | heal 场景未实战测试 | 2425 |
| R69 | 深度验证 | 性能基线 + CO 真实运行 | CO 从未真实运行 | 2425 |
| R70 | 发布 | 全量验证 + v0.12.0-rc | — | 2430+ |

---

## 五、与 v0.11.0-rc 的经验对比

| 经验 | v0.11.0-rc | v0.12.0-rc 对策 |
|------|-----------|----------------|
| 测试反向调用问题 | benchmark 从测试内调 pytest 超时 | R62 benchmark_fn 使用文件作用域 |
| SelfArchitect 太薄 | 只产生字符串占位 | R61 结构化提案 + 4 种 change_type |
| CodeGen→Deploy 链路断 | CodeGenerator 只产 stub | R64 AST 级真实代码生成 |
| CO 从未真实优化 | sandbox_test 一直模拟 | R62 + R69 真实 benchmark 运行 |
| 混沌测试仅模拟 | 无真实故障注入 | R68 真实故障（删文件/破坏依赖/语法错误） |

v0.11.0-rc 证明了**安全网足够坚固**。v0.12.0-rc 要在安全网内**真正飞起来**。
