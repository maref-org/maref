> """CLI commands for AI-Native IP content production.
  
> Usage:
>     maref ip scaffold                        # Create asset directory structure
>     maref ip produce --theme cyberpunk-neko  # Full pipeline: char → scripts → assemble
>     maref ip produce-char --theme fantasy-elf # Character profile only
>     maref ip write-script --char-id sylvara --episode 1
>     maref ip assemble --char-id sylvara
>     maref ip validate --char-id sylvara
>     maref ip status                          # Show asset database status
>     maref ip list-themes                     # Available character themes
> """
  
! from __future__ import annotations
  
! from pathlib import Path
  
! import typer
! from rich import box
! from rich.console import Console
! from rich.panel import Panel
! from rich.table import Table
  
! from maref.production.asset_scaffolder import AssetScaffolder
! from maref.production.character_factory import CharacterFactory
! from maref.production.content_engine import ContentEngine
  
! ip_app = typer.Typer(name="ip", help="AI-Native IP production commands", no_args_is_help=True)
! console = Console()
  
  
! @ip_app.command()
! def scaffold() -> None:
!     """Create the IP asset directory structure (§2.3)."""
!     s = AssetScaffolder()
!     result = s.scaffold()
!     console.print(
!         Panel(
!             f"[green]Asset database scaffolded[/green]\n"
!             f"  Base: {result['base']}\n"
!             f"  Directories: {result['directories_created']}",
!             title="IP Asset Scaffold",
!         )
!     )
  
  
! @ip_app.command()
! def produce(
!     theme: str = typer.Option("cyberpunk-neko", "--theme", "-t", help="Character theme ID"),
!     episodes: int = typer.Option(2, "--episodes", "-e", help="Number of episodes to generate"),
!     char_id: str = typer.Option(
!         "", "--char-id", help="Custom character ID (defaults to theme name)"
!     ),
! ) -> None:
!     """Run full production pipeline: scaffold → character → scripts → assemble → validate."""
!     engine = ContentEngine()
!     result = engine.run_full_pipeline(
!         theme_id=theme, char_id=char_id or None, episode_count=episodes
!     )
  
!     console.rule(f"[bold cyan]Production Pipeline: {result.char_id}[/bold cyan]")
  
!     for step in result.steps:
!         console.print(f"  [green]{chr(10003)}[/green] {step}")
  
!     p = result.profile
!     console.print(f"\n[bold]Character:[/bold] {p['name']} ({p['archetype']})")
!     console.print(f"  [dim]Profile:[/dim] {p['directory']}")
  
!     for s in result.scripts:
!         console.print(
!             f"  [dim]Episode {s['episode_number']}:[/dim] {s['title']} ({s['total_duration_s']}s)"
!         )
!         console.print(f"    [dim]Script:[/dim] {s['script_path']}")
  
!     a = result.assembled
!     if a:
!         console.print(f"\n[bold]Export:[/bold] {a['export_dir']} ({a['size_kb']} KB)")
!         console.print(f"  [dim]Manifest:[/dim] {a['manifest_path']}")
  
!     v = result.validation
!     if v:
-         console.print(
!             f"\n[bold]Hypothesis Validation:[/bold] "
!             f"[{'green' if v.overall_pass else 'red'}]{'PASS' if v.overall_pass else 'FAIL'}[/]"
!         )
-         for name, passed, detail in [
!             ("H1", v.h1_pass, v.h1_detail),
!             ("H2", v.h2_pass, v.h2_detail),
!             ("H3", v.h3_pass, v.h3_detail),
!         ]:
-             icon = "[green]PASS[/green]" if passed else "[red]FAIL[/red]"
-             console.print(f"  {icon} {name}: {detail[:80]}")
  
!     console.print(f"\n[dim]Pipeline completed in {result.total_duration_s:.2f}s[/dim]")
  
  
! @ip_app.command(name="produce-char")
! def produce_char(
!     theme: str = typer.Option("cyberpunk-neko", "--theme", "-t", help="Character theme ID"),
! ) -> None:
!     """Generate a character profile only."""
!     engine = ContentEngine()
!     result = engine.run_character_only(theme_id=theme)
!     console.print(f"[green]Character generated:[/green] {result['name']}")
!     console.print(f"  Profile: {result['profile_path']}")
!     console.print(f"  Directory: {result['directory']}")
  
  
! @ip_app.command(name="write-script")
! def write_script(
!     char_id: str = typer.Option(..., "--char-id", help="Character ID"),
!     episode: int = typer.Option(1, "--episode", "-e", help="Episode number"),
! ) -> None:
!     """Generate an episode script for a character."""
!     engine = ContentEngine()
!     result = engine.run_script_only(char_id=char_id, episode=episode)
!     console.print(
!         f"[green]Script written:[/green] Episode {result['episode_number']}: {result['title']}"
!     )
!     console.print(f"  Path: {result['script_path']}")
!     console.print(f"  Scenes: {result['scene_count']}, Duration: {result['total_duration_s']}s")
  
  
! @ip_app.command()
! def assemble(
!     char_id: str = typer.Option(..., "--char-id", help="Character ID"),
!     episode: int = typer.Option(0, "--episode", "-e", help="Episode number (0 = all)"),
! ) -> None:
!     """Package character assets into deployable export."""
!     engine = ContentEngine()
!     result = engine.run_assemble_only(char_id=char_id, episode=episode or None)
!     console.print(f"[green]Export assembled:[/green] {result['export_dir']}")
!     console.print(f"  Scripts: {result['script_count']}")
!     console.print(f"  Profile: {'yes' if result['has_profile'] else 'no'}")
!     console.print(f"  Size: {result['size_kb']} KB")
  
  
! @ip_app.command()
! def validate(
!     char_id: str = typer.Option(..., "--char-id", help="Character ID"),
! ) -> None:
!     """Validate character against H1/H2/H3 hypotheses."""
!     engine = ContentEngine()
!     result = engine.run_validate_only(char_id=char_id)
!     console.print(f"[bold]Hypothesis Validation: {result.char_id}[/bold]")
-     for name, passed, detail in [
!         ("H1", result.h1_pass, result.h1_detail),
!         ("H2", result.h2_pass, result.h2_detail),
!         ("H3", result.h3_pass, result.h3_detail),
!     ]:
-         icon = "[green]PASS[/green]" if passed else "[red]FAIL[/red]"
-         console.print(f"  {icon} {name}: {detail}")
-     console.print(
!         f"\n  Overall: [{'green' if result.overall_pass else 'red'}]{'PASS' if result.overall_pass else 'FAIL'}[/]"
!     )
  
  
! @ip_app.command()
! def status() -> None:
!     """Show IP asset database status."""
!     s = AssetScaffolder()
!     status = s.get_status()
!     if not status["exists"]:
!         console.print(
!             "[yellow]IP asset database not initialized. Run 'maref ip scaffold' first.[/yellow]"
!         )
!         return
!     console.print(f"[bold]IP Asset Database:[/bold] {status['base']}")
!     console.print(
!         f"  Characters: {len(status['characters'])} — {', '.join(status['characters']) if status['characters'] else '(none)'}"
!     )
!     console.print(
!         f"  Storylines: {len(status['storylines'])} — {', '.join(status['storylines']) if status['storylines'] else '(none)'}"
!     )
!     console.print(f"  Exports: {status['export_count']}")
!     console.print(f"  Size: {status['size_mb']} MB")
  
  
! @ip_app.command(name="list-themes")
! def list_themes() -> None:
!     """Show available character themes."""
!     cf = CharacterFactory()
!     themes = cf.list_themes()
!     table = Table(title="Available Character Themes", box=box.SIMPLE)
!     table.add_column("Theme ID", style="cyan")
!     table.add_column("Name", style="green")
!     table.add_column("Archetype", style="yellow")
!     for t in themes:
!         table.add_row(t["id"], t["name"], t["archetype"])
!     console.print(table)
  
  
! @ip_app.command(name="full-cycle")
! def full_cycle(
!     doc_path: str = typer.Argument(..., help="Path to requirements doc"),
!     theme: str = typer.Option("cyberpunk-neko", "--theme", "-t", help="Character theme to produce"),
!     episodes: int = typer.Option(2, "--episodes", "-e", help="Episodes to produce"),
!     iterations: int = typer.Option(3, "--iterations", "-n", help="MAREF evolution cycles"),
! ) -> None:
!     """Run MAREF develop-feature then produce real IP content."""
!     from maref.integration.feature_dev.doc_ingestor import MarkdownDocIngestor
!     from maref_lite.percv_cli import develop_feature
  
!     console.rule("[bold magenta]Phase 1: MAREF Document Analysis[/bold magenta]")
!     ingestor = MarkdownDocIngestor()
!     doc = ingestor.ingest(str(Path(doc_path).expanduser().resolve()))
!     console.print(f"  Document: [cyan]{doc.title}[/cyan]")
!     console.print(f"  Stages: {doc.metadata['detected_stages']}")
!     console.print(f"  Hypotheses: {len(doc.hypotheses)}")
  
!     develop_feature(doc_path=doc_path, iterations=iterations, output="", verify=True)
  
!     console.rule("[bold magenta]Phase 2: Real Content Production[/bold magenta]")
!     produce(theme=theme, episodes=episodes, char_id="")
  
!     console.rule("[bold magenta]Phase 3: Content Validation[/bold magenta]")
!     engine = ContentEngine()
!     from maref.production.asset_scaffolder import AssetScaffolder
  
!     s = AssetScaffolder()
!     chars = s.get_status().get("characters", [])
!     if chars:
!         for cid in chars:
!             result = engine.run_validate_only(char_id=cid)
-             icon = "[green]PASS[/green]" if result.overall_pass else "[red]FAIL[/red]"
-             console.print(
!                 f"  {icon} {cid}: H1={result.h1_pass} H2={result.h2_pass} H3={result.h3_pass}"
!             )
  
!     console.print(
!         "[bold green]Full cycle complete: Analysis → Production → Validation[/bold green]"
!     )
  
  
! def main() -> None:
!     ip_app()
