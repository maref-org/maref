version: "3.9"

services:
  telemetry:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9100:9100"
    environment:
      - MAREF_TELEMETRY_DB=/data/events.db
    volumes:
      - maref_telemetry_data:/data
    restart: unless-stopped

volumes:
  maref_telemetry_data:
