# MAREF 仓库缺口审计报告

**审计日期**: 2026-08-05
**审计范围**: 全仓声明文件（AGENTS.md / STATE.yaml / CHANGELOG / mission.json）对照实际代码、测试、质量门禁
**审计基线**: dev 分支 HEAD `7a87f939`（v0.52 P0 五项治理缺口收口）
**审计方法**: research-verification v2.0（全景情报 → 交叉验证 → 盲点扫描 → 优先级分级）

---

## 0. 执行摘要

共发现 **11 项缺口**：🔴 Critical 1 / 🟠 High 4 / 🟡 Medium 4 / 🟢 Low 2。

**核心结论**: 代码实现质量高（1680 回归全绿、gov/security 1381 passed），但**声明文件严重滞后于代码**——v0.52 已合入但版本未 bump，v0.51 mission.json 仍为 planned 状态，AGENTS.md 测试/覆盖率声明过时。存在"代码≠文档"的发布链断裂风险，以及一处**真实运行缺陷**（desktop dry_run 默认值）。

---

## 1. 缺口清单

### 🔴 CRITICAL

| # | 缺口 | 证据 | 影响 |
|---|------|------|------|
| G-01 | **版本发布链断裂**: HEAD 已合入 v0.52 治理缺口收口，但 6 个版本文件仍为 0.51.0，CHANGELOG 无 v0.52 记录 | commit `7a87f939` vs `__init__.py`/`pyproject.toml`/`STATE.yaml`/`gui/package.json`/`Cargo.toml`/`AGENTS.md` 全为 0.51.0；`grep v0.52 CHANGELOG.md` = 0 | 违反版本一致性锁规范；用户安装 v0.51.0 拿不到已合入的治理修复；Release 链断裂 |

### 🟠 HIGH

| # | 缺口 | 证据 | 影响 |
|---|------|------|------|
| G-02 | **mission.json 状态未回写**: v0.51 mission 显示 `status=planned` / `all_milestones_completed=false` / `completed_features=0` / `current_milestone=M1`，而 STATE.yaml 声称 13/13 完成 | `.missions/v0.51.0-enterprise-value/mission.json` vs `STATE.yaml:64` | 违反 AGENTS.md Handoff Discipline 第 4 条；状态追踪失真 |
| G-03 | **desktop dry_run 运行缺陷**: `test_dry_run_default_false_when_no_env` 失败——`BrowserController.dry_run` 无 env 时默认为 True 而非 False | `tests/desktop/test_browser_controller.py` 7 failed / 25 passed 实测 | 生产代码行为与测试契约不符；无 env 环境默认进入 dry-run，可能静默吞掉真实操作 |
| G-04 | **质量门禁声明与实际不符**: AGENTS.md 声称 ruff + mypy strict 全过，实际 `ruff check` 3 errors（I001+F821×2），`mypy src/maref` 15 errors / 7 files | `ruff`/`mypy` 实测 | 声称的门禁未达成；trust_engine_v2.py F821 `UnifiedAuditRecord` 未定义可导致运行时 NameError |
| G-05 | **API 前缀规范大规模违反**: AGENTS.md 规定所有 API 路由用 `/api/v1/` 前缀，实际 30+ 个路由无前缀（`/api/a2a/*`、`/api/compliance/*`、`/api/immunity/*`、`/api/agents` 等） | `grep -rhoE '"/api/[^"]*"'` 实测 | 违反 Coding Convention；生态/客户端契约不一致 |

### 🟡 MEDIUM

| # | 缺口 | 证据 | 影响 |
|---|------|------|------|
| G-06 | **AGENTS.md 测试数量声明过时**: 声称 9547+ collected / 5968 passed，实际收集 15723 tests | `pytest --collect-only` 实测 15723 | 文档误导；低估测试规模 |
| G-07 | **覆盖率声明无新鲜证据**: STATE.yaml 声称 80.31%，唯一佐证 `coverage_full.json` 为 08-04 13:41 旧快照；本次全量收集覆盖 27.38%（pytest 阈值 50% FAIL） | `coverage_full.json` mtime + `pytest --cov` 实测 | 覆盖率指标失真，无法支撑 50%+ 门禁声明 |
| G-08 | **DEPRECATED 模块未清理**: 10 个标记 DEPRECATED 的模块仍在源码树且多数被大量引用（audit_logger 36 处、registry 82 处、trust_engine 15 处、credential 22 处） | `grep -rl DEPRECATED` + 引用计数实测 | 维护负担；新旧双轨并行，调用方易混淆 |
| G-09 | **lastfailed 缓存 670 项**: `.pytest_cache/v/cache/lastfailed` 记录 670 个失败测试（desktop/evolution/sentinel 等），多数未确认是否已修复 | 实测文件读取 | 回归基线不可信；CI 通过声明缺乏证据 |

### 🟢 LOW

| # | 缺口 | 证据 | 影响 |
|---|------|------|------|
| G-10 | **根目录遗留调试/演示脚本被跟踪**: 13 个 `test_phase*.py`、`debug_*.py`、`cli_percv_demo.py`、`integration_demo.py` 等入库，部分不在 .gitignore | `git ls-files` 实测 | 仓库卫生；pre-commit 漏检 |
| G-11 | **25-MAREF 治理缺口审计报告未入库**: v0.52 commit message 引用"25-MAREF-治理三维度缺口审计报告-20260805"，但仓库内无此文件 | `find` 实测为空 | 审计证据链断裂；依据不可追溯 |

---

## 2. 实测数据（关键证据）

```
测试收集:       15723 tests collected (42.65s)
gov+security:   1381 passed / 2 skipped
sidecar wiring: 1085 passed
desktop:        7 failed / 25 passed
ruff:           3 errors (I001 + F821×2)
mypy src/maref: 15 errors / 7 files (依赖缺失 + 1 syntax)
覆盖率:         全量收集 27.38% < 50% 阈值 FAIL
               coverage_full.json 旧快照 80.31% (08-04)
版本:           HEAD=v0.52, 全版本文件=v0.51.0
```

---

## 3. 修复建议

| 优先级 | 动作 | 文件 | 估计 |
|--------|------|------|------|
| P0 | 版本 bump 0.51.0 → 0.52.0（7 文件）+ CHANGELOG 条目 + tag | `__init__.py`/`pyproject.toml`/`STATE.yaml`/`gui/*`/`CHANGELOG.md`/`AGENTS.md` | 0.5d |
| P0 | 修复 desktop dry_run 默认值 + 回归 7 失败 | `maref/desktop/browser_controller.py` | 0.5d |
| P0 | 回写 mission.json 完成状态（13/13, M1-M4） | `.missions/v0.51.0-enterprise-value/mission.json` | 0.5d |
| P1 | 修复 ruff 3 errors + mypy src/maref 15 errors | `trust_engine_v2.py` 等 7 文件 | 1d |
| P1 | 新跑全量覆盖率并更新 STATE.yaml + 记录佐证 | `coverage_full.json` + `STATE.yaml` | 2d |
| P1 | 决策：路由前缀补 `/api/v1/` 或豁免声明（选一） | `src/sidecar/*.py` | 1-3d |
| P2 | 清理 DEPRECATED 模块或标注迁移计划 | `integration/audit_logger` 等 | 3-5d |
| P2 | 清理根目录调试脚本 / 补 .gitignore | 根目录 13 文件 | 0.5d |
| P2 | 补充 25-MAREF 审计报告入库 | `docs/audit-reports/` | 0.5d |

---

**审计员**: opencode (MAREF 仓库缺口审计)
**审计结论**: ⚠️ 未通过 — 存在 1 项 Critical（版本链断裂）+ 1 项真实运行缺陷，修复 G-01~G-05 后建议复审
